/// Shared corner-radius constants so every card/button stays visually consistent.
class AppRadius {
  AppRadius._();

  static const double card = 20;
  static const double button = 16;
  static const double chip = 12;
  static const double sheet = 28;
}
